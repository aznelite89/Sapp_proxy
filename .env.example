# Server port (Render sets PORT automatically)
PORT=3000

# Azure Function endpoint INCLUDING the code query param (keep secret server-side)
# Example:
# AZURE_ENDPOINT=https://searay-email-func.azurewebsites.net/api/SendBackOrder?code=REPLACE_WITH_NEW_KEY
AZURE_ENDPOINT=
SHOPIFY_API_SECRET=
